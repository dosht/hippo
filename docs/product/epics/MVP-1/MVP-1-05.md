---
title: hippo-remember Skill Cross-Project Install
status: done
priority: high
updated: 2026-04-18
id: MVP-1-05
epic: MVP-1
effort: S
depends: ['MVP-1-04']
---

### S5: hippo-remember Skill Cross-Project Install

**As an agent in any project**, I want the hippo-remember skill available globally so that I can
invoke it without being inside the Hippo project.

#### Acceptance criteria

- [ ] .claude/skills/hippo-remember/ exists and the skill description is accurate (when to invoke, what it delegates to)
- [ ] `mkdir -p ~/.claude/skills && ln -s ~/src/hippo/.claude/skills/hippo-remember ~/.claude/skills/hippo-remember` is documented in developer-guide
- [ ] Skill is verified present in another project's Claude session skill list
- [ ] Skill correctly delegates to the memory subagent via `claude -p --working-directory ~/src/hippo`

#### Architect Notes

Wave 3b (sequential after S4). Stub to verify: `.claude/skills/hippo-remember/SKILL.md` (exists; verify only, do not modify). The symlink at `~/.claude/skills/hippo-remember` is a one-time manual step; document it in the developer-guide as the installation command. Gotcha: the skill delegates via `claude -p --working-directory ~/src/hippo`; the path is hardcoded as an accepted tradeoff for a single-developer system (see risk R3 in design doc). Design doc ref: `docs/product/epics/MVP-1/design.md` S5.
