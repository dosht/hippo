---
title: Memory Subagent Refinement
status: done
priority: high
updated: 2026-04-18
id: MVP-1-04
epic: MVP-1
effort: S
depends: ['MVP-1-01', 'MVP-1-02']
---

### S4: Memory Subagent Refinement

**As an agent**, I want the memory subagent definition to be correct and tested against real gold
entries so that it returns structured, accurate answers without hallucinating beyond what entries
contain.

#### Acceptance criteria

- [ ] .claude/agents/memory.md is reviewed and updated: confirm model is haiku, confirm the `qmd query` command uses `--collection hippo --json -n 5`, confirm response format includes Answer, Confidence, Last validated, and Stale warning fields
- [ ] At least 3 test queries run manually against the seeded gold entries, each returning the correct structured format
- [ ] At least 1 test query for a topic NOT in gold returns "No relevant entries found" (not a hallucinated answer)
- [ ] Agent description accurately states when to use vs. not use memory (operational knowledge yes, curated doc conventions no)

#### Architect Notes

Wave 3a (parallel with S3 and S8.5). Stub to flesh out: `.claude/agents/memory.md` (exists). Critical: model must remain `haiku` (latency-sensitive interactive path; pipeline uses `sonnet-4-5`). Add the `HIPPO_HOME` env var resolution so the working directory is not hardcoded (sub-task described in design doc S5 S4). Boundaries: do NOT touch the `hippo-remember` skill (S5), do NOT modify gold entries. Design doc ref: `docs/product/epics/MVP-1/design.md` S4.
