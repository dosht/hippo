# Test Plan: MVP-2-01

Story: MVP-1 Bug Fixes (quota recovery, sidecar gate, project identity, history flood, thread consolidation)
Target: api
Test file: `tests/test_ingest.py`, `tests/test_extract.py`, `tests/test_reconcile.py`, `tests/test_compact.py`
Run: `cd /Users/mu/src/hippo && python -m pytest tests/ -v -k "not TestCallClaudeCompact"`

## T1: shared errors module (FOUNDATION)

Given `scripts/errors.py` exists in the repo:
- [x] T1.1 `scripts/errors.py` defines `QuotaExhaustedError` as a single shared class
- [x] T1.2 `compact.py`, `extract.py`, and `reconcile.py` all import from `scripts.errors` (no local copies)
- AC: 1.1 (indirectly — no failed rows if errors shared)

## T2: Quota recovery in extract.py

Depends on: T1

Given a silver entry in the manifest and `extract.py` is invoked:
- [x] T2.1 `QuotaExhaustedError` raised mid-run leaves no `failed` rows in manifest — verified by `TestExtractQuotaRecovery::test_quota_wall_leaves_no_failed_rows`
- [x] T2.2 `extract.py` exits with code 0 on quota wall — verified by same test (return value 0)
- [x] T2.3 In-flight session retains `status: silver` after quota wall — verified by test checking manifest unchanged
- AC: 1.1, 1.4

## T3: Quota recovery in reconcile.py

Depends on: T1

Given `reconcile.py judge` is invoked and quota wall is simulated:
- [x] T3.1 `QuotaExhaustedError` raised in judge exits with code 0 — `TestReconcileJudgeQuotaRecovery::test_quota_wall_exits_zero`
- [x] T3.2 No proposal file is written when quota wall hit — `TestReconcileJudgeQuotaRecovery::test_quota_wall_does_not_write_proposals`
- AC: 1.2, 1.4

## T4: Manifest reset verification (Issue 1 AC3)

Given the manifest at `/Users/mu/src/hippo/manifest.jsonl`:
- [x] T4.1 `jq '[.[] | select(.status=="failed")] | length'` returns 0 (confirmed: 32,044 bronze, 88 silver, 30 gold, 3 skipped-large, 0 failed)
- AC: 1.3

## T5: Sidecar gate in ingest.py (FOUNDATION)

Given `ingest.py` is invoked with a session JSONL:
- [x] T5.1 Session with no sidecar entry is rejected — `TestSidecarGate::test_no_sidecar_session_rejected` (logged at INFO, not written to manifest)
- [x] T5.2 Session with matching sidecar entry is admitted — `TestSidecarGate::test_matching_sidecar_session_admitted`
- [x] T5.3 Absent `sessions.jsonl` (no sidecar file) returns empty index and bypasses gate — `TestLoadSidecarIndex::test_absent_file_returns_empty`
- AC: 2.2, 2.3, 2.4

## T6: session_start.py is the only new file (Issue 2 AC1)

Given the git log for this story:
- [x] T6.1 `scripts/hooks/session_start.py` exists as the hook
- [x] T6.2 Bronze/silver/gold/QMD/hippo-remember skill are unchanged (verified by story scope — only scripts/*.py modified)
- AC: 2.1

## T7: project_id resolution in session_start.py

Given `scripts/hooks/session_start.py` is executed:
- [x] T7.1 With a git remote, `project_id = sha1(remote_url)` — code confirmed at line 67
- [x] T7.2 With local git (no remote), `project_id = sha1("local:" + repo_realpath)` — code confirmed at line 69
- [x] T7.3 With no git context, `project_id = null` — code confirmed at line 63 fallback
- [x] T7.4 `ingest.py` reads `project_id` from sidecar and writes to manifest row — `TestProjectIdAndThreadId::test_project_id_and_thread_id_in_manifest`
- AC: 3.1, 3.2, 3.3, 3.4

## T8: Ingest cutoff (Issue 5)

Depends on: T5

Given `ingest.py` is invoked with sidecarred sessions:
- [x] T8.1 Session with `started_at: 2026-04-27` is skipped under default cutoff `2026-04-28` — `TestIngestCutoff::test_session_before_cutoff_skipped`
- [x] T8.2 Session with `started_at: 2026-04-28` is admitted under default cutoff — `TestIngestCutoff::test_session_on_cutoff_day_admitted`
- [x] T8.3 Session with no sidecar is rejected before cutoff check — `TestIngestCutoff::test_unsidecarred_session_rejected_before_cutoff_check`
- [x] T8.4 Default cutoff is `2026-04-28` when both config and env absent — `TestLoadIngestCutoff::test_default_when_no_config_and_no_env`
- [x] T8.5 `HIPPO_INGEST_FROM` env var overrides default — `TestLoadIngestCutoff::test_env_var_overrides_default`
- [x] T8.6 `~/.hippo/config` INI file is read and env overrides it — `TestLoadIngestCutoff::test_config_file_read`
- AC: 5.1, 5.2, 5.3, 5.4, 5.5

## T9: thread_id through the pipeline (Issue 6)

Depends on: T5, T7

Given a session with `thread_id` in its sidecar/manifest row:
- [x] T9.1 `compact.py` writes `thread_id` to silver YAML frontmatter — `TestSilverFrontmatterThreadId::test_thread_id_present_in_frontmatter`
- [x] T9.2 `compact.py` serializes `null` for absent `thread_id` — `TestSilverFrontmatterThreadId::test_thread_id_null_when_absent`
- [x] T9.3 `extract.py` carries `thread_id` from silver frontmatter to gold entry — confirmed by `_apply_python_owned_fields` at line 575 and `TestExtractSession::test_silver_frontmatter_metadata_propagated`
- [x] T9.4 `reconcile.py` cluster step co-clusters three sessions sharing same `thread_id` into one cluster — `TestThreadIdClustering::test_three_sessions_same_thread_id_one_cluster`
- [x] T9.5 Sessions with different `thread_id` stay in separate clusters — `TestThreadIdClustering::test_different_thread_ids_stay_separate`
- AC: 6.1, 6.2, 6.3, 6.4, 6.5
