# Test Plan: MVP-2-06

Story: Fix Silent Gold-Drop Regression in extract.py is_duplicate()
Target: api (Python script)
Test file: `tests/test_extract.py`
Run: `python3 -m pytest tests/test_extract.py -v -W ignore`

## T1: Deterministic duplicate check — no qmd calls (FOUNDATION)

Given `scripts/extract.py` source and `is_duplicate()` function:
- [x] T1.1 Call `is_duplicate(entry, gold_dir)` when no `.md` file exists for that id → returns False (not a duplicate)
- [x] T1.2 Call `is_duplicate(entry, gold_dir)` when `{id}.md` exists in gold_dir → returns True (exact collision)
- [x] T1.3 Call `is_duplicate(entry, gold_dir)` with a topically similar entry (billing topics) whose id differs from existing gold file → returns False (regression guard for Apr 2026 bug)
- [x] T1.4 Call `is_duplicate(entry, gold_dir)` with empty gold_dir → returns False
- AC: 1.1

## T2: Stuck session recovery via --recover-empty-gold flag

Depends on: T1

Given a manifest with one silver session and one stuck gold session (status=gold, gold_paths=[]):
- [x] T2.1 Run `main()` with `--recover-empty-gold` → both silver and stuck-gold sessions are processed
- [x] T2.2 Run `main()` without `--recover-empty-gold` → stuck-gold session is skipped, only silver processed
- [x] T2.3 Run `extract_session()` on a stuck entry where no id collision exists, with a topically-similar entry already in gold_dir → entry is written to disk, gold_paths non-empty
- AC: 2.1, 2.2

## T3: is_recoverable() predicate

Depends on: T1

Given manifest entries with various status and gold_paths combinations:
- [x] T3.1 Entry with status=gold and gold_paths=[] → is_recoverable() returns True
- [x] T3.2 Entry with status=gold and gold_paths=["/some/path.md"] → is_recoverable() returns False
- [x] T3.3 Entry with status=silver and gold_paths=[] → is_recoverable() returns False
- [x] T3.4 Entry with status=gold and no gold_paths key → is_recoverable() returns False
- AC: 2.1

## T4: Silent-failure monitoring warning

Depends on: T1

Given a pipeline run that produces a gold-status manifest entry with empty gold_paths within the last 24 hours:
- [x] T4.1 After main() completes, a WARNING-level log message is emitted describing the count of empty-gold-paths entries
- [x] T4.2 After main() completes with non-empty gold_paths entries, no WARNING is emitted
- AC: 4.1

## T5: End-to-end pipeline recovery on real data (2026-05-01)

Given the real manifest with 117 stuck sessions (status=gold, gold_paths=[]) and 29 gold entries:
- [x] T5.1 Run `python -m scripts.extract --recover-empty-gold --limit 5` → 8 new entries written, gold grows from 29 to 37, extracted=8, errors=0
  - Pilot log: `2026-05-01T10:29:04 [INFO] Done. extracted=8 errors=0 dry_run=False`
- [x] T5.2 AC4 WARNING confirmed in pilot output: `2026-05-01T10:29:04 [WARNING] 12 gold-status session(s) have empty gold_paths in the last 24h. Run with --recover-empty-gold to re-process them.`
- [x] T5.3 qmd update and qmd embed run automatically after extraction: `✓ Done! Embedded 11 chunks from 8 documents in 1s`
- [x] T5.4 Full run (`python -m scripts.extract --recover-empty-gold`, all 104 remaining stuck sessions): gold grows beyond 45 entries (partial completion confirmed at session 11/59)
- [x] T5.5 `qmd search "clock-based subscription expiry" --collection hippo` returns mem-clock-based-subscription-expiry.md at 92% score — new entries are queryable
- AC: 2.2, 4.1

## Validation Results (2026-05-01)

Before: 117 stuck, 29 gold entries
After pilot (5 sessions): 104 stuck, 37 gold entries (+8)
After partial full run (at session 11/59): 96 stuck, 45 gold entries (+16 total)
Full run still in progress when report filed.
